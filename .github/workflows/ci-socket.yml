name: LINKLIAN-SOCKET CI

on:
  push:
    branches: ["main", "staging"]
    paths:
      - "**/*.go"
      - "go.mod"
      - "go.sum"
      - "Dockerfile"
      - "docker-compose.yml"
      - ".github/workflows/ci-socket.yml"
      - "go-ci.sh"          #Rerun CI when .sh updated
      - "cmd/**"
      - "internal/**"
      - "pkg/**"
      - "nginx/**"
      - "examples/**"

  pull_request:
    branches: ["main", "staging"]
    paths:
      - "**/*.go"
      - "go.mod"
      - "go.sum"
      - "Dockerfile"
      - "docker-compose.yml"
      - ".github/workflows/ci-socket.yml"
      - "go-ci.sh"          #Rerun CI when .sh updated
      - "cmd/**"
      - "internal/**"
      - "pkg/**"
      - "nginx/**"
      - "examples/**"

  workflow_dispatch:

concurrency:
  group: ci-socket-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  security-events: write # needed for SARIF upload (gosec)

jobs:
  ci:
    name: Go CI + Security (linux)
    runs-on: ubuntu-latest

    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ---------- Secret scan ----------
      - name: Secret scan (gitleaks)
        uses: gitleaks/gitleaks-action@v2
        with:
          args: detect --redact --no-git

      # ---------- Go setup ----------
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      # ---------- SAST (gosec) ----------
      - name: SAST (gosec) -> SARIF
        run: |
          set -euo pipefail
          go install github.com/securego/gosec/v2/cmd/gosec@latest
          "$HOME/go/bin/gosec" -fmt=sarif -out gosec.sarif ./...

      - name: Upload SAST report (SARIF)
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: gosec.sarif

      # ---------- Go CI (standalone) ----------
      - name: Go fmt check
        run: |
          set -euo pipefail
          files="$(gofmt -l . || true)"
          if [[ -n "$files" ]]; then
            echo "Files not formatted:"
            echo "$files"
            echo "Fix with: gofmt -w ."
            exit 1
          fi

      - name: Go vet
        run: |
          set -euo pipefail
          go vet ./...

      - name: Go mod download
        run: |
          set -euo pipefail
          go mod download

      - name: Go build
        run: |
          set -euo pipefail
          go build -v ./...

      - name: Unit tests
        run: |
          set -euo pipefail
          go test -v ./... -count=1
